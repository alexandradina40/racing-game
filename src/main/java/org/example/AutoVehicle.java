package org.example;

public class AutoVehicle extends Vehicle
{
    Engine engine;     // a car has an engine

    public AutoVehicle(Engine engine)
    {
        this.engine=engine; // current object , the above one
    }
}
